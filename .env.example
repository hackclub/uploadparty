# Example env vars. Copy to .env and adjust for your environment.
# Backend CORS will allow this origin. Used by backend/config/config.go (FRONTEND_URL)
FRONTEND_URL=https://uploadparty.hackclub.com

# Frontend canonical base URL (used across metadata/sitemap/robots)
# Place in site/.env.local or site/.env.production for Next.js
NEXT_PUBLIC_SITE_URL=https://uploadparty.hackclub.com
