# Root .env example for UploadParty (centralized)
# Copy/rename to .env for local dev and customize. Do NOT commit real secrets.
# This single file is used by BOTH the backend (Gin) and the frontend (Next.js) in dev.

# Backend API
PORT=8080
GIN_MODE=debug
FRONTEND_URL=http://localhost:3000
JWT_SECRET=change_me

# Postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=uploadparty
DB_PASSWORD=your_local_db_password
DB_NAME=uploadparty_db
DB_SSL_MODE=disable

# Redis
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=

# Google Cloud Storage (optional)
GCP_PROJECT_ID=
GCS_BUCKET=uploadparty-beats
# Path to your service account JSON (used by Google client libraries)
# Place the file under ./secrets and keep it out of git
GOOGLE_APPLICATION_CREDENTIALS=./secrets/gcp-service-account.json

# External license directory (generic; keep secrets private)
# LICENSES_PROVIDER can be 'none' (default) or 'airtable'
LICENSES_PROVIDER=none
# LICENSES_TOKEN is a generic bearer token (for Airtable, use API key)
LICENSES_TOKEN=
# LICENSES_DSN is an opaque string; example for Airtable: base=appXXXXXXXXXXXX;table=Licenses
LICENSES_DSN=

# Frontend (Next.js)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Auth0 (Frontend + API integration)
# Generate your own values; NEVER commit real secrets.
AUTH0_SECRET=replace-with-a-strong-random-secret
AUTH0_BASE_URL=http://localhost:3000
AUTH0_ISSUER_BASE_URL=https://your-tenant.us.auth0.com
AUTH0_CLIENT_ID=replace-with-your-auth0-client-id
AUTH0_CLIENT_SECRET=replace-with-your-auth0-client-secret

# Native Instruments API (if used)
NI_API_KEY=
NI_API_URL=https://api.native-instruments.com
